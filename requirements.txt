# ── AI / LLM ──
langgraph>=0.2.0
langchain-openai>=0.2.0
langchain-core>=0.3.0

# ── 配置 ──
pyyaml>=6.0
python-dotenv>=1.0

# ── 数据库 ──
sqlalchemy>=2.0
psycopg[binary]>=3.1

# ── 日志 ──
loguru>=0.7

# ── 数据校验 ──
pydantic>=2.0

# ── HTTP / 飞书 ──
httpx>=0.25

# ── Web 服务 ──
fastapi>=0.110
uvicorn>=0.29

# ── 数据处理 ──
# pandas>=2.0       # 如需 kline 数据处理
# numpy>=1.24       # 如需数值计算

# ── 数据库迁移 ──
# alembic>=1.13     # 如需 PostgreSQL 迁移