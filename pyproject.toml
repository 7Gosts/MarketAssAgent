[project]
name = "MarketAssAgent"
version = "0.1.0"
description = "LangGraph ReAct agent for multi-market technical analysis"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]