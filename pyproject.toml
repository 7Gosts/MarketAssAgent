[project]
name = "MarketAssAgent"
version = "0.1.0"
description = "Native tool-calling agent for multi-market technical analysis"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "postgres: tests that require a live PostgreSQL service",
]
